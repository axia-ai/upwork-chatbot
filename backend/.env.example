# Copy to .env and fill in. Never commit your real key.
# The app reads ANTHROPIC_API_KEY from the environment; it is never hardcoded.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Set to true to force deterministic demo mode even when a key is present.
DEMO_MODE=false
